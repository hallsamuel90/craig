---
"craig-cli": patch
---

Fix project workspace PR check refreshes so target PR checks update through the manual refresh path and tracked target refresh failures are reported instead of being treated as missing PRs.
