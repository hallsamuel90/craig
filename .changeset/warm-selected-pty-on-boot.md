---
"craig-cli": patch
---

Warm the restored selected PTY tab on shell boot when the daemon has no cached rows, so Craig renders the last selected agent or terminal instead of showing an extra attach prompt.
